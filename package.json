{
  "name": "sabbatical-allocation",
  "version": "1.1.0",
  "private": true,
  "description": "Single-file worksheet that divides sabbatical seats across colleges by the Hamilton method in rounds.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node build.js",
    "check": "node build.js && node --test test/*.test.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
